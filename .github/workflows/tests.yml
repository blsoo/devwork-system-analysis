name: DevWork checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile prototype
        run: python -m py_compile prototype/devwork_core.py prototype/test_devwork_core.py
      - name: Run behaviour tests
        working-directory: prototype
        run: python -m unittest -v test_devwork_core.py
